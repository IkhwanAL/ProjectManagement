import { ReactElement } from "react";

const Footer = (): ReactElement => {
	return (
		<div className="bg-slate-300 max-h-full bottom-0 fixed w-full text-center">
			asd
		</div>
	);
};

export default Footer;
